#include<iostream>
using namespace std;

int main(){
    // int age = 20;
    
    // (age >= 18) ? cout << "You can vote" : cout << "You cannot vote";

    int x=10;
    int y=20;

    int result = (x > y) ? x : y;

    cout << "Result: " << result << endl;
    
    return 0;
}